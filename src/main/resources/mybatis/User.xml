<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.supbio.peento.mapper.UserMapper">

    <select id="findAllUser" resultType="com.supbio.peento.models.entity.User">

      select id, user_name AS userName, create_time AS createTime, update_time AS updateTime
      from t_user;

    </select>

    <select id="findOne" resultType="com.supbio.peento.models.entity.User">

        select id, user_name AS userName, create_time AS createTime, update_time AS updateTime
        from t_user WHERE id = #{id};

    </select>


</mapper>
